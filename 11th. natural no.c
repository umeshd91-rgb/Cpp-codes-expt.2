#include<stdio.h>
 int main(){
      int n, sum = 0, 1;
      
      printf("Enter a value of n:");
      scanf("%d", %n);
      for(i == 1; i <= n; i ++){
          sum += i;}
          printf("Sum of first %d natural numbers is:%d ,n,sum");
  
 }