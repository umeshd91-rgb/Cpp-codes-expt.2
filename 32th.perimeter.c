#include <stdio.h>

int main()
{
    float l, w, perimeter;
    printf("Enter length: ");
    scanf("%f",&l);
     printf("Enter length: ");
    scanf("%f",&l);
   perimeter = 2 * (l + w);
   
    printf("Perimeter is: %f ", perimeter);
    return 0;
}